org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Works around a Windows-specific Kotlin incremental-compiler bug ("Could not
# close incremental caches") that reliably breaks plugin builds
# (image_picker_android, shared_preferences_android, etc.) on this OS.
# Costs a bit of rebuild speed, not correctness. Safe to remove if building
# on Linux/Mac, where this bug doesn't occur.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
