# Test-only dependencies — never installed in the production Docker image.
# Install alongside requirements.txt in a local/dev/CI venv:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest
pytest-asyncio
httpx
